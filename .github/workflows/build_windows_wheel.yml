name: Build+Upload Windows Python Wheels
on:
  push:
    branches:
      - master
    tags:
      - '*'
  pull_request:
    branches:
      - master

jobs:
  build_windows_wheels:
    name: Build Windows wheels
    runs-on: windows-2022
    defaults:
      run:
        shell: bash
    strategy:
      matrix:
        python_version: ['3.9', '3.10', '3.11', '3.12', '3.13']
    outputs:
      perform_pypi_upload: ${{ steps.perform_pypi_upload_check.outputs.perform_pypi_upload }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          submodules: 'recursive'
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python_version }}
      - uses: msys2/setup-msys2@v2
        with:
          update: true
          install: >-
            mingw-w64-x86_64-gcc-fortran
            mingw-w64-x86_64-suitesparse
            mingw-w64-x86_64-lapack
            mingw-w64-x86_64-openblas
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-pkg-config
      - name: Put MSYS2_MinGW64 on PATH
        run: echo "D:/a/_temp/msys64/mingw64/bin" >> $GITHUB_PATH
      - name: Build Wheels (Python < 3.12, numpy.distutils with MinGW)
        if: ${{ matrix.python_version == '3.9' || matrix.python_version == '3.10' || matrix.python_version == '3.11' }}
        env:
          UMFPACK_INCLUDE: "D:/a/_temp/msys64/mingw64/include/suitesparse;D:/a/_temp/msys64/mingw64/include"
          UMFPACK_LIB: "D:/a/_temp/msys64/mingw64/lib"
        run: |
          cd python/
          pip install wheel setuptools "numpy>=1.20,<2.0" scipy
          # Create distutils config to force MinGW compiler
          mkdir -p $HOME
          cat > $HOME/pydistutils.cfg << 'EOF'
          [build]
          compiler=mingw32
          [build_ext]
          compiler=mingw32
          EOF
          # Also create setup.cfg in the project
          cat > setup.cfg << 'EOF'
          [build]
          compiler=mingw32
          [build_ext]
          compiler=mingw32
          EOF
          # Build with explicit compiler specification
          python setup.py build_ext --compiler=mingw32 --fcompiler=gnu95 bdist_wheel
          ls dist/
          mv dist/ ../
          cd ../
      - name: Build Wheels (Python >= 3.12, meson)
        if: ${{ matrix.python_version == '3.12' || matrix.python_version == '3.13' }}
        env:
          UMFPACK_INCLUDE: "D:/a/_temp/msys64/mingw64/include/suitesparse;D:/a/_temp/msys64/mingw64/include"
          UMFPACK_LIB: "D:/a/_temp/msys64/mingw64/lib"
          FC: gfortran
          CC: gcc
          CXX: g++
          PKG_CONFIG_PATH: "D:/a/_temp/msys64/mingw64/lib/pkgconfig"
        run: |
          cd python/
          pip install build meson-python meson ninja numpy scipy
          python -m build --wheel
          ls dist/
          mv dist/ ../
          cd ../
      - name: Check If the Build Version Exists on PyPI
        id: perform_pypi_upload_check
        run: |
          ./.github/check-pypi-upload.sh
      - name: Prepare Wheels for Upload
        uses: actions/upload-artifact@v4
        with:
          name: windows-wheels-${{ matrix.python_version }}
          path: dist/

  upload_windows_wheels:
    needs: build_windows_wheels
    runs-on: ubuntu-latest
    if: ${{ github.repository_owner == 'fangq' && needs.build_windows_wheels.outputs.perform_pypi_upload == 1 && github.event_name != 'pull_request'}}
    permissions:
      id-token: write
    steps:
      - name: Download Wheels from Build Job
        uses: actions/download-artifact@v4
        with:
          pattern: windows-wheels-*
          merge-multiple: true
          path: dist/
      - name: Upload packages to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          verify_metadata: false
          verbose: true
          skip_existing: true